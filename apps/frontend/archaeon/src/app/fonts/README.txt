Drop your Kelvetica Nobis font file(s) here.

Expected filenames (next/font/local will try in order):
  KelveticaNobis.woff2   ← preferred
  KelveticaNobis.otf     ← fallback

Both are configured in src/app/layout.tsx.

The build will NOT fail if these files are missing — a system-font fallback is active.

LICENSE REMINDER:
  Check the FontSpace page before using beyond a hackathon demo:
  https://www.fontspace.com/kelvetica-nobis-font-f19686
  Verify whether the license is personal-use-only or free-for-commercial.
